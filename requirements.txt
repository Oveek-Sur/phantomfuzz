aiohttp>=3.8

# Big async throughput win (auto-detected at runtime; safe to skip on Windows).
uvloop>=0.19; platform_system != "Windows"

# Optional: headless-browser rendering for SPA discovery (auto --render,
# web --render-discover / --render-seed). Install with:
#   pip install playwright && playwright install chromium
# playwright>=1.40
